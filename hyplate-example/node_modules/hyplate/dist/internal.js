import { remove } from "./core.js";
import { __DEV__ } from "./util.js";
export const comment = (message) => new Comment(__DEV__ ? message : "");
export const withCommentRange = (message) => {
    const begin = comment(` ${message} begin `);
    const end = comment(` ${message} end `);
    return [
        () => {
            remove(begin);
            remove(end);
        },
        [
            begin,
            end,
            () => {
                const range = new Range();
                range.setStart(begin, begin.length);
                range.setEnd(end, 0);
                range.deleteContents();
                range.detach();
            },
        ],
        () => [begin, end],
    ];
};
//# sourceMappingURL=internal.js.map